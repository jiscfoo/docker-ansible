Ansible in Docker
=================

Basic ansible installtion.

Invokation
----------

For example...

```
docker run --rm -ti -v "${PWD}":/a -v "${HOME}/.ssh/id_rsa:/root/.ssh/id_rsa" jiscfoo/ansible'
```
